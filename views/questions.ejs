<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title><%= title %></title>
</head>
<body>
    <%- include('header.ejs') %>
    <div class="my-6 mx-10">
        <div class="flex gap-2 py-4 justify-between">
            <h5 class="text-2xl"><%= title %> - Questions</h5>
            <div class="bg-purple-800 rounded w-fit px-2 py-1 hover:bg-purple-600">
                <a href="/elections/<%= id %>/questions/create" class="text-white">+ Add a new question</a>
            </div>
        </div>
        <div class="flex-auto">
            <div class="mx-4">  
                <p class="text-purple-600"><%= (questions.length==0)?"You haven't added any questions yet!":"" %></p>
                <ol class="list-decimal">
                    <% for (let i=0; i<questions.length; i++) { %>
                        <li>
                            <div class="flex items-center w-fit my-2 px-2 py-1 rounded hover:bg-purple-50">
                                <a href="" class="m-2 text-sm text-purple-600 "><%= questions[i].question %></a>
                            </div>
                        </li>
                    <% } %>
                </ol>
            </div>
        </div>
    </div>
</body>
</html>